#include "CS299_list.h"

int display_allButFirst(node * head)
{
	node * current = head;

	int value;
	int value2;

	value = head->data;
	cout << value << endl;
	current = current->next;
	value2 = curent->data;

	while(current != null)
	{	
		value2= next->data;
		if (head != value2)
		{
			cout << value2 << endl;
		}
		current = current->next;
		
	}


}
return 0;