#include "CS299_arr.h"

int display_all_but_last(head ** node)
{
	int = 0;
 	return move_in_array(head, i);
}

int move_in_array(head ** node, int i, cur);
{
	while(!head)
		i++;
		return traverse_display(head[i])
}

int traverse_display(head ** node)
{
	while(!current)
	cout << "->" << current->data;
	return traverse_display(current->next);
}